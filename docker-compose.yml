version: '3.9'
services:
  api:
    build: .
    environment:
      - NODE_ENV=production
    ports:
      - "${PORT:-3000}:3000"
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/status"]
      interval: 30s
      timeout: 10s
      retries: 5
